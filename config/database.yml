

default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: deviseA_development
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: deviseA_test

production:
  <<: *default
  database: deviseA_production
  username: deviseA
  password: <%= ENV['DEVISEA_DATABASE_PASSWORD'] %>
